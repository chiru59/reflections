What happens when you initialize a repository? Why do you need to do it?
	A git repository will be created for the present directory. But it will not make 
any comming. git status can be used to find the status of the repo

How is the staging area different from the working directory and the repository?
What value do you think it offers?
	Staging area allows us to commit a bundle of files. It provided more flexiblity
by giving an option. We can add files from working directory to stating area to be commited.
This allows one commit per a logical change.

How can you use the staging area to make sure you have one commit per logical
change?
	We can add only the files with changes to the staging area.By this way you can use
staging area to commit once for a logical change.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
	Branches can be used when you want to make a different version,experimental change
or large change whithou effecting the present code

How do the diagrams help you visualize the branch structure?
	There can be many branches at a time. And commits can be on any branch. So it is 
easier to understand the commits/branches by visualizing.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
	Every files is merged after analysis. Representing all the branches the merges
in a picture can make us understand easily.

What are the pros and cons of Gitâ€™s automatic merging vs. always doing merges
manually?
	Manual merging gives us more flexibility. 
